[project]
name = "aivoa-complaint-copilot"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.34,<1", "sqlalchemy>=2,<3", "psycopg[binary]>=3.2,<4", "langgraph>=1,<2", "groq>=0.30,<1", "pypdf>=5,<7", "python-multipart>=0.0.20", "pydantic-settings>=2,<3"]
[dependency-groups]
dev = ["pytest>=8", "httpx>=0.28"]
[tool.pytest.ini_options]
pythonpath = ["."]
